<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>To DO</title>
  </head>
  <body>
    <h1>Todos</h1>
    <form action="/" method= "POST">
      <input type="text" name="newTodo" value="What to do?"/>
        <button type="submit" name="Todo"><Add</button>
    </form>
      <ul>

        {{ #toDos }}
        <li>
          {{ #complete }} <del> {{ /complete }}
          {{ text }}
          {{ #complete }} </del> {{ /complete }}
          <form action="/mark-complete/{{ id }}" method="POST">
            <button type="sumbit">mark complete</button>
          </form>
        </li>
        {{ /toDos }}

      </ul>
    <form action="/" method="get">
    <ul>
      {{ #complete }}
      <li> {{.}} </li>
      {{ /complete }}
    </ul>
  </form>
  </body>
</html>
